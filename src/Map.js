import React, { Component } from 'react';
import PropTypes from 'prop-types'

class Map extends Component {
	render () {
		
		return (
			<div id="map">Map</div>
		)
	}
}

export default Map